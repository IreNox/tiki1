using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Drawing;
using System.Data;
using System.Linq;
using System.Text;
using System.Windows.Forms;

namespace TikiEngine.Editor.Controls
{
    public partial class ucLevelProperties : ucMainProperties
    {
        public ucLevelProperties()
        {
            InitializeComponent();
        }


        /*if (propertys.SelectedObject == null) return;

        if (propertys.SelectedObject.GetType() == typeof(Level))
        {
            
        }
        else if (propertys.SelectedObject.GetType() == typeof(TileMap))
        {
            _stateMap.CurrentLevel.Sort();
            _loadLevel();
        }*/
    }
}
